name: Build & Release Installers

env:
  APP_NAME: GateTIK
  VERSION: 2.2.0

on:
  push:
    branches:
      - main

jobs:
  # Windows, Linux, & Android Installers
  build:
    name: Build ${{ matrix.installer }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            installer: msi
            task: packageReleaseMsi
            artifact_name: installer-msi
            artifact_path: "**/build/compose/binaries/main-release/msi/*.msi"
            os_name: windows
            arch: amd64

          - os: ubuntu-latest
            installer: deb
            task: packageReleaseDeb
            artifact_name: installer-deb
            artifact_path: "**/build/compose/binaries/main-release/deb/*.deb"
            os_name: linux
            arch: amd64

          - os: ubuntu-latest
            installer: apk
            task: assembleRelease
            artifact_name: installer-apk
            artifact_path: "**/build/outputs/apk/release/*.apk"
            os_name: android
            arch: ""

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Write local.properties
        shell: bash
        run: | 
          echo "BACKEND_URL=${{ secrets.BACKEND_URL }}" >> local.properties
          echo "ANDROID_INSTALLATION_URL=${{ secrets.ANDROID_INSTALLATION_URL }}" >> local.properties
          echo "LATEST_RELEASE_INFO_URL=${{ secrets.LATEST_RELEASE_INFO_URL }}" >> local.properties
          echo "WEB_RTC_PLAYER_BASE_URL=${{ secrets.WEB_RTC_PLAYER_BASE_URL }}" >> local.properties
          echo "VERSION=${{ env.VERSION }}" >> local.properties

      - name: Generate BuildConfig
        run: ./gradlew generateBuildConfig
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}

      - name: Decode Android keystore
        if: matrix.installer == 'apk'
        shell: bash
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > GateTIK.jks

      - name: Package ${{ matrix.installer }}
        run: ./gradlew ${{ matrix.task }}
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}
          KEYSTORE_FILE: ${{ github.workspace }}/GateTIK.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEYSTORE_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
          KEYSTORE_KEY_PASSWORD: ${{ secrets.KEYSTORE_KEY_PASSWORD }}

      - name: Rename installer
        shell: bash
        run: |
          FILE=$(find . -path "${{ matrix.artifact_path }}" | head -n 1)
          DIR=$(dirname "$FILE")
          ARCH="${{ matrix.arch }}"
          if [ -n "$ARCH" ]; then
            NAME="${{ env.APP_NAME }}-${{ env.VERSION }}_${{ matrix.os_name }}_${ARCH}.${{ matrix.installer }}"
          else
            NAME="${{ env.APP_NAME }}-${{ env.VERSION }}_${{ matrix.os_name }}.${{ matrix.installer }}"
          fi
          mv "$FILE" "$DIR/$NAME"

      - name: Upload ${{ matrix.installer }} artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error

  # GitHub Release
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all installers
        uses: actions/download-artifact@v4
        with:
          path: installers
          merge-multiple: true

      - name: List downloaded files
        run: ls -lh installers/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}
          name: v${{ env.VERSION }}
          body: |
            Automated release from commit ${{ github.sha }}

            **Changes in this release:**
            ${{ github.event.head_commit.message }}
          files: installers/**/*
          fail_on_unmatched_files: true